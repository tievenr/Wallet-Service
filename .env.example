# --- Database Credentials ---
# The root password for the MySQL container
MYSQL_ROOT_PASSWORD=rootpassword
# The username for the application database
DB_USER=wallet_user
# The password for the application database
DB_PASSWORD=wallet_pass
# The name of the main application database
DB_NAME=wallet_db

# --- App Configuration ---
# Options: development, testing, production
APP_ENV=development
# Secret key for JWT/Security (change this in production!)
SECRET_KEY=dev-secret-key-change-in-production

# --- SQLAlchemy Connection URLs ---
# This URL is for the app running INSIDE Docker (using the service name 'db')
DATABASE_URL=mysql+pymysql://wallet_user:wallet_pass@db:3306/wallet_db

# This URL is for running Pytest from your LOCAL terminal (using 'localhost')
TEST_DATABASE_URL=mysql+pymysql://wallet_user:wallet_pass@localhost:3306/test_db